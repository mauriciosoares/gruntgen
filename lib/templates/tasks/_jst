  // =============================================
  // JST
  // docs: https://github.com/gruntjs/grunt-contrib-jst
  config.jst = {
    compile: {
      options: {
        templateSettings: {
          interpolate : /\{\{(.+?)\}\}/g
        }
      },
      files: {
        "path/to/compiled/templates.js": ["path/to/source/**/*.html"]
      }
    }
  };

