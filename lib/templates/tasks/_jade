  // =============================================
  // Jade
  // docs: https://github.com/gruntjs/grunt-contrib-jade
  config.jade = {
    compile: {
      options: {
        data: {
          debug: false
        }
      },
      files: {
        "path/to/dest.html": ["path/to/templates/*.jade", "another/path/tmpl.jade"]
      }
    }
  };

